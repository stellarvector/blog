# Hugo build output
public/

# Hugo cache
resources/
.hugo_build.lock

# Hugo module / npm caches, when built the way CI does
.cache/

# OS generated files
.DS_Store
Thumbs.db

node_modules

hugo_stats.json